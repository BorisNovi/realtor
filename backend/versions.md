Задачи себе:
- вынести ответ с access & refresh в отдельный шаблон
- сортировка по цене - как быть? ⁉️
- фильтрация по city и country, собрать эти фильтры и вывести в ответ на запрос list. в табличном виде!! + количество. 

06.02.26 - ИМПОРТ
➕ Базовый импорт с ограничением количества строк в таблице. 

26.02.26 - ЭКСПОРТ
➕ Рабочий стриминг-экспорт БД по общим полям БЕЗ СПЕЦИФИК. 

24.02.26
✍🏼 Ошибки капсом.
✍🏼 Убраны лишние комментарии. 

20.02.26
➕ Расширена модель адреса.

13.02.26 
🛠️ Фикс поиска (контакты)
✍🏼 Вернул специфики в подборки путем переброски с ListSerializer на маппер дочернх.

12.02.26 - БОЛЬШАЯ ПЕРЕСБОРКА
🛠️ Починены все фильтры на карте: 
    - перевод на кверисет, 
    - вьюха изменена на `viewsets.ModelViewSet`. 
    - Пагинация автоматическая. 
✍🏼 Пересобран модуль контактов: 
    - вьюха оптимизирована под ModelViewSet, убрано много лишнего кода 
    - уникальность номера проверяется только в контексте конкретного юзера
    - юзер имеет доступ только к своим контактам
✍🏼 Пересобран модуль подборок:
    - Все то же самое, что и для контактов.  
    - сортировка по дате работает, но остался вопрос наименования поля в БД
    - изображение профиля компании и ее имя приходят в ответе. 
✍🏼 Пересобран модуль авторизации:
    - пересобрана модель юзера и пайплайн авторизации. 

11.02.26 
🛠️ Починены все фильтры в Листе, функция возвращает кверисет вместо листа (лучшая оптимизация). 
✍🏼 Перевел CatalogListView на viewsets.ModelViewSet. По идее, это лучше, чем городить кастом. 
✍🏼 Квери, определяющий, что юзер не будет видеть чужих записей - вынесен в mixin (`CurrentUserQuerysetMixin`). 

10.02.26
🛠️ Фикс редактирования фото у объектов (передавал старый аргумент в url). 
✍🏼 Убедился, что имя объекта будет отдаваться в любом случае
✍🏼 Пробросил сериалайзер отображения списка объектов в подборки. 
➕ Права доступа к List. 

05.02.26 
✍🏼 Переписан сериализатор карты - теперь все объекты всех типов отдаются нормально.
➕ Добавлена возможность присваивать имя объектам недвижимости
✍🏼 Исправлено удаление: раньше удалятор обращался к моделям по их названию, а теперь работ идет с Property. 

04.02.26 - ПЕРЕДЕЛКА РАБОТЫ С КОНТАКТАМИ
✍🏼 Теперь контакты необязательны - я разрешил их на уровне модели (+ cascade_delete=false) и сериализатора
✍🏼 При создании и редактировании объекта теперь всегда передается просто ID контакта, без доп. полей.  
✍🏼 Ошибка, при которой контакты создавались из раза в раз, ушла сама собой

03.02.26 - ПЕРЕСТРОЙКА БД, ДОБАВЛЕНЫ ОСТАЛЬНЫЕ ТИПЫ ОБЪЕКТОВ С ПОЛЯМИ-ЗАГЛУШКАМИ
➕ Модели и сериализаторы под новые типы созданы, все работает, но поля такие же, как у квартир.  
✍🏼 Модель изменена на `Polymorphic`, нумерация объектов сквозная. 
✍🏼 Обновил методы во вьюхах.
✍🏼 Переписал взаимодействие с листом объектов

02.02.26 - ПЕРЕСТРОЙКА
✍🏼 Оптимизация: сократил объем кода в сериализаторах при помощи использования ModelSerializer, 
    что избавило от необходимости прописывать лишние валидации и заново перечислять поля модели.  
✍🏼 Перераспределение ролей: теперь вьюхи и сериализаторы работают так, как надо. 
➕ Переработан дочерний сериализатор и его связь с общим. Теперь они согласуются правильно.
    Базовый сериалайзер вызвается дочерним и ничего не знает о `specifics`, 
    вместо этого оба создают свои инстансы, которые дополняются в базе.   
➕ Выбор типа для объекта доступен: вьюха считывает `propertyType` и вызывает нужный дочерний сериалайзер.  
➕ Добавлена атомарность процесса создания объектов.

29.01.26 - УПРОЩЕНО ХРАНЕНИЕ ФОТО
- Улучшен сериализатор профиля. 
- Допилен модуль изменения аватарки профиля, корректный перенос в постоянное хранилище 
- Фотки сваливаются в одну общую папку

21.01.26 - РЕДАКТИРОВАНИЕ ПРОФИЛЯ БЕТА 
- Добавлен модуль профиля с возможностью менять пароль, имя и лого компании (не уходит в постоянное хранилище!!)

19.01.26 – МГНОВЕННЫЙ ЛОГАУТ JWT
➕ Мгновенный кик пользователя на всех устройствах.
 - Убрана старая логика `Django-сессии (login(), request.session, UserSession)` для авторизации.
 - Создано поле `last_logout_at` в модели `User` для контроля момента глобального выхода.
 - Разработан кастомный auth-класс `KillSwitchJWTAuthentication`:
  - Проверяет `iat` access-токена против `last_logout_at`.
  - `Access` токены, выданные до `last_logout_at`, сразу отклоняются (`401`).
  - `Refresh` токены пропускаются, но при `logout_all` блэклистятся через `OutstandingToken` → `BlacklistedToken`.
✍🏼 `SigninView/SigninSerializer` возвращают только `JWT`-токены без сессий.
✍🏼 `LogoutAllView`: обновляет `last_logout_at` и блэклистит все `refresh-токены`.
➕ Повышена безопасность: даже если `access` или `refresh` украдены, пользователь мгновенно кикается со всех устройств.

08.01.25 - ЗАВЕРШЕНИЕ ВСЕХ СЕССИЙ
➕ BETA: Завершение всех сессий. 
          - Создана модель `UserSession`. 
          - При логине в таблице создаются сессии, и к ним привязывается user_id. 
          - При обращении к `/logout-all` из куков достается токен, по которому уничтожаются все связанные с ним записи в БД.
✍🏼 Сериализатор входа возвращает объект юзера, а не словарь (улучшение).   

26.12.25 - КВАДРАТ КООРДИНАТ
✍🏼 Допилена логика выдачи объектов в заданных координатах; изменен формат ответа. 

20.12.25 - 
✍🏼 Скорректирована логика работы с лимитами на загрузку файлов (доработан сериализатор). 
➕ BETA: Добавлен `Pillow` для сжатия изображений. Работает норм. 
✍🏼 Некоторые переменные вынесены в `settings.py`.  
➕ Выдача объектов внутри квадрата координат.

17.12.25 - Лимиты для загрузки файлов по кол-ву и кач-ву. 
➕ Задал лимит на вес загружаемых файлов на сервер:
    - глобально = 10 мб (в любом случае),
    - локально = 2 мб (только фото через ЭП). 
   Также ограничение на кол-во загружаемых файлов в размере 25 штук. 

08.12.25 - Починеные сортировки, контакт с комментом, и прочее. 
➕ Если объект удален, его ID удаляется из подборок, где был использован. Реализовал с помощью post_save (DRF signals)
✍🏼 Если подборка недоступна (`available=false`), ссылка на нее все равно придет. Теперь ссылка - это token. 
✍🏼 Упрощена логика поиска публичного листинга. 
🛠️ Починена сортировка подборок. Пришлось снова делать маппинг из верблюдского кейса в змеиный.   
➕ Сортировка Объектов недвижимости теперь работает. 
➕ Сортировка по умолчанию для листингов, ОН и контактов - по дате (нов->стар). 
➕ У контактов теперь есть поле "коммент".

04.12.25 - ФИКС Shared Facilities
✍🏼 Фронт слал Shared Facilities с kitchen и bathroom без префикса. 
    Адаптировал бэк для корректного восприятия: внес корректив в `prepare_property_data`.   
➕ Отдаю дату в ответе к подборкам.

03.12.25 - ФИКС РЕДАКТИРОВАНИЯ СПЕЦИФИК
🛠️ Подтянул к актуальному методу работы с сериализатором обновление объекта по части специфик.  

02.12.25 - НАСТРОЙКИ ДОСТУПА К ЛИСТИНГАМ
➕ Разобрался с публичными ссылками листингов. Если `available` в БД public_link = true, тогда даем доступ. Если нет - 404.  
➕ Контакты из ответа для публичных запросов выпиливаются функцией `remove_contacts`.  
✍🏼 Поправил порядок путей. 

01.12.25 - ЗАВЕРШЕНА РАБОТА В МОДУЛЕ ЛИСТИНГОВ
➕ Закончил с полями в модели для листингов: имя и лого компании, а также ссылки - доступны или нет. 
➕ Добавлены валидации полей в сериализаторе
➕ Токенизация в урлах

27.11.25 - ХЕШИРОВАНИЕ ПАРОЛЕЙ
➕ Пароли хэшируются. Как при локальной разработке, так и при запуске на проде через Редис. 

24.11.25 - ЗАВЕРШЕНА РАБОТА С МЕДИА
➕ Теперь можно добавлять и убирать медиа (привязка/отвязка URLs) через update. 
➕ Все специфики без данных в булевых полях воспринимаются нормально. Баг с необходимой галочкой исправлен.
➕ передаем `zoningType` в листе объектов
🛠️ Фильтры починены: работают и воспринимают как dotted-keys (price.currency), так и bracket-keys. 

19.11.25 - ИЗМЕНЕНИЕ КОНТАКТА В ОБЪЕКТЕ
🛠️ Теперь можно переназначать контакты в ОН. 
🛠️ Починен `zoningType`

18.11.25 - ОТДЕЛЬНЫЙ СЕРИАЛИЗАТОР ДЛЯ ЛИСТА
✍🏼 Написан отдельный сериализатор для листа всех объектов недвижимости.  

11.11.25 - ПОБЕДА НАД СПЕЦИФИКАМИ
✍🏼 Полностью переделан (упрощен) поток **создания объектов**. Теперь структуризация происходит на месте, в `to_representation`. 
✍🏼 Удалены костыльные интерфейсы
🛠️ Наконец исправлена запись специфик. Проблема была в неправильной обработке входящих/исходящих ключей. 
⚠️ Не проверены остальные участки, скорее всего будут баги в других методах
⚠️ Временно убраны сторонние модели объектов кроме flat. 

11.11.25 - Фиксы
🛠️ Исправлена сериализация ответа для листинга: 
    Был написан тестовый чистый флэт-сериализатор во избежание использования костыльных интерфейсов (откажись от них в будущем)
🛠️ Вернул конвертер для переменных snake -> camel и назад в джанго-настройках.     

07.11.25 
➕ Возможность дополнительно к имени выслать айдишники под запись. 
    В ответе возвращается объект подборки со списком айдишников.   
➕ При запросе подборки отдаю подборку с раскрытыми объектами недв., входящими в нее.
    Для этого был написан сериализатор. Пока что все очень просто:
    1. Записываем в БД айдишники в `property_object_ids` тупым массивом []. 
    2. Ищем их функцией `get_property_objects` через сериализатор `FlatSerializer` (пока что).  

06.11.25 (ПОЛНОФУНКЦИОНАЛЬНЫЙ ЛИСТИНГ)
➕ Полноценный CRUD для листингов

05.11.25 (ЗАГОТОВКА ДЛЯ ЛИСТИНГА)
➕ Добавлена возможность создавать подборки с именем. МВП.

03.11.25
🛠️ Исправлено удаление контактов. Теперь точно умеем слушать запрос с квери-строкой из `params`. Без форм-даты. 

03.11.25
🛠️ Исправлен чек сессии. Были проблемы с перезаписью REST_FRAMEWORK в настройках. 
    Сука, не перезаписывай его, блядь, тварь криворукая. Смотри чтобы блоки не дублировались!!! 
✍🏼 Изменен способ удаления контактов. Удаление одного или нескольких в одном приеме. 
    Теперь передача кандидатов на выбывание осуществляется через form-data.  
🛠️ Исправлена загрузка фото. Файлы перемещаются корректно. В будущем осторожнее с путями, гринго. 

02.11.25 [ФИКС ПРЕОБРАЗОВАНИЯ ДАННЫХ]
✍🏼 Поправил ответ при загрузке файлов. Урлы больше не вложены, ссылка отдается целиком
🛠️ Починил конвертацию кэмел кейса в снейк кейс.
  Была проблема в том, что входящий camelCase не парсился в снейк, который ждет джанго. 
  Почему - хрен знает. Разобраться так и не смог. Возможно, данные где-то переопределялись.

  Нашел библиотеку для преобразования данных "djangorestframework-camel-case", 
  добавил оттуда `underscoreize` в `to_internal_value` сериализатора `CatalogCreateSerializer`, 
  чтобы преобразовать ключи перед валидацией. 

  В будущем при схожих ошибках:
  - добавляй `underscoreize` в проблемные сериализаторы,  
  - логируй `request.data` и `parser_classes`, 
  - исключай конфликты кастомных парсеров и тестируй запросы с корректным `Content-Type: application/json`. 

  В идеале нужно прийти к глобальному решению через парсеры, чтобы избежать дублирования кода. Но это мелочи.

  ⚠️ Возможны схожие проблемы в других сериализаторах! 
  Если другие сериализаторы (например ContactSerializer) получают camelCase, 
  добавь underscoreize в их to_internal_value аналогично.  
Побаловался с colorama. 
🛠️ Починил доступы к блоку user_auth без токена. 

29.10.25 [ФИКС СЛЭШЕЙ И ДОСТУПОВ]
🛠️ Раз и навсегда решена проблема доступов к эндпоинтам со слэшами в конце/начале пути.
    Выясненно, что:
    - В корневых путях слэши не нужны ни в начале, ни в конце путей.
    - в модульных (дочерних) путях слэши нужны только в начале.     
🛠️ Улучшена работа с доступом к эндпоинтам. Теперь без JWT токена нельзя получить доступ к приватным путям. 
    Доступ к блоку авторизации оставлен публичным.
    Если нужно сделать ЭП приватным -- ставим в начало класса ограничивающие правила. 
    Если нужно сделать ЭП публичным, ставим аналогичные строки, но разрешающие права. 
    Не забываем импортировать permission classes!    
✅ Решена проблема из прошлого коммита с кверизами. Дело было в слэше между конечной точкой и параметрами после "?".  

28.10.25 [ФИКСЫ]
🛠️ Исправлена отправка авторизованных запросов на /file. 
    Была ебля с Simple JWT (я забыл что использую его для авторизации). 
🛠️ Исправлена невозможность работы без слэша на конце пути. 
‼️ нужно починить квери-запросы в модуль контактов. 404 вответна /list/?.

26.10.25 [ПОДГРУЗКА ФОТО]
➕ Добавлена возможность подгружать файлы..

28.09.25 [УДАЛЕНИЕ КОНТАКТОВ]
➕ Добавил удаление пачкой (через квери-параметр). 
🛠️ Починил поиск по части слова (а-ля полнотекстовый, но нихуя не полнотекстовый как оказалось)
‼️ Ошибка каскадного удаления не выдается, пока что втихаря удаляем все связанные объекты. 

27.09.25 
➕ Добавил сортировку контактов по имени и дате добавления

18.09.25 [UPGRADE CONTACTS]
✍🏼 Исправил формат выдачи контактов на табличный с тоталом - дополнена вьюха. 
✍🏼 Убрал валидатор имен, оставив только ограничение на кол-во символов.  
♻️ Сделал пагинацию контактов. Формат: /contact/list/?first=0&rows=15

17.09.25 [UPGRADE CONTACTS]
➕ Добавил валидацию на создание контактов с поддержкой португальского, испанского и грузинского языков. 
➕ Добавил поля "дата создания" и "доп.телефон". 
✍🏼 Исправил роутинг для контактов 
‼️ Еще не сделана пагинация, фильтрация и сортировка контактов.

11.09.25 [UPGRADE CONTACTS]
➕ Добавил методы удаления и получения контакта/списка контактов. 
‼️ Еще не сделана пагинация, фильтрация и сортировка контактов. 

18.07.25 [SPECIFICS FIX]
🛠️ Починил запись специальных (дополнительных) полей. Теперь БД их запоминает. 
- Дополнительно навел небольшой порядок в флоу. Теперь поступающие специфики раскладываются в файле-парсере.   

16.07.25 [UPDATE FIX]
🛠️ Починил обновление данных ОН (PUT/UPDATE). 
✍🏼 Перекинул логику работы с PUT & PATCH в файл catalog_detail_view, и теперь все детальные махинации с конкретным ОН в 1 месте. 


08.07.25 [INTERFACES FIX]
🛠️ Починил штучную выдачу объекта, убрав пару костылей. 
🛠️ Починил запись объекта адреса в БД. Теперь объект полноценный. 
    По ходу дела столкнуться с проблемой, что где-то в коде, а конкретно в каталог сериалайзере, 
    принудительно записывался адрес в строку, что недопустимо в текущей концепции адреса-объекта.

07.07.25 [INTERFACES]
➕ Добавил маски (интерфейсы) как на фронте, согласно которым бэк рисует ответ.
  + Адрес (принимает объект, трансформируем в строку согласно требованию БД, потом возвращаем снова объект)
  + Контакт (живет отдельно от апки!!!)
  + Прайс (теперь не нужно указывать каждый раз, как его возвращать - шаблон есть в интерфейсе, просто вызываем его оттуда)
  + Спецификации для квартир (только !!!). 
  + Собираем все поля в property_responce.ру: 
      а. если создаем/вызываем 1 объект - то все поля;
      б. если вызываем список - то сокращенно, без спецификаций (обе функции лежат рядом).  
🗑️ Удалил файл CatalogItemSerializer за ненадобностью, т.к. теперь есть интерфейсы.  
⚠️ Создание обеъекта и запрос списка - работают. Остальное нужно проверять.      

05.07.25 [CONTACT FIX]
- Пофиксил логику создания объектов и работу с контактами. 
  Если при создании объекта пытаемся создать контакт с занятым телефоном, но другим именем = 400.
  Если отправляем Имя и телефон, которые уже есть = 201 (достаем из базы и тупо привязываем айдишник оного)


19.06.25 [CONTACT]
- полностью переделал работу с контактами. Теперь это отдельное приложение с собственными скриптами, где логика централизована.  
- Убрал необходимость добавления контакта при создании обеъекта. 
  В процессе пришлось поебаться, так как сложилась следующая ситуация: во всех моделях контакт стоял как  необязателен, но на уровне БД было наоброт.
  Из-за этого была ошибка при попытке создать объект. 

13.06.25 [DETAILS FIXED, STATUS UPDATE]
- Починил штучную выдачу. Создал для этого отдельный сериализатор (read only). Теперь все отдается в соответствии с интерфейсов. 
- Докинул в модельку квартиры недостающие поля. Остальные не трогал. 

- Добавил к catalog_detail_view функцию с методом патч для обновления статуса недвижимости.  

09.06.25 [DETAILS]
- Добавил штучную выдачу объектов со всеми деталями

09.06.25 [UPDATE]
- Добавил функцию апдейта объектов. Смена типа недвижимости пока недоступна из-за технических сложостей (разные модели).
- Исключил из выдачи мягко удаленные объекты.

08.06.25 [DELETE]
- Добавлена возможность удаления объектов: 
  Запрошенным объектам присваиваем флаги "is_deleted" & "deleted_at", 
  затем специальной утилитой (периодичность работы утилиты установим позже) подчищаем из базы, опираясь на установленный "срок годности".
- Удаление происходит только с токеном авторизации. 

02.06.25 [REFRESH FIX]
- изменил метод с пут на пост. 

19.05.25 [FILTERS]
- Сделал фильтрацию. Проверил только площадь, цену и валюту. Все фильтры хорошо друг с другом работают.

11.05.25 [PAGINATION]
- Сделал пагинацию. Вроде работает корректно. 

03.04.25 [better responces]
- Улучшил работу с разными сценариями авторизации, добавив логику распознавания разных ошибок. 
  При регистрации:
    422: Проблемы с паролем: 
           - меньше 8 символов,  
           - не содержит спец.символов,
           - не содержит верхний регистр,
           - поля не совпадают.
           - повторяет части емейла (ник или домен)
           - пустые поля
    400: Проблемы с мейлом:
           - Недопустимые символы
           - поле пустое
           - запрещенный домен
           - 
- Теперь мы не палим наличие мейлов в системе и в любом случае говорим "Если такой мейл существует, мы вышлем на него письмо" и:
  - Если он правда существует, ничего не шлем (защита от спама)
  - Если не существует, то шлем 
- Добавил коды ошибок рядом с сообщениями. 

23.04.25 [CONTACT]
- Добавлена логика работы с контактами. При создании объявления можем заполнить необязательные данные о контакте. 
  Когда отправляем запрос на создание с данными о контакте, проверяем, существует ли такой контакт (по номеру телефона). 
    1. Если контакт существует, присваиваем новому ОН айди этого контакта в базе данных. 
    2. Если контакта не существует, создаем, а затем присваиваем айди свежего контакта. 
- При выдаче результатов /CATALOG отдаем данные о контакте в конце ответа в красивом формате, включая поля id, name, phone.
- Пришлось поебаться с миграциями! Будь осторожен, блеать, когда работаешь с моделями... 

22.04.25 [CATALOG LIST]:
- Появилась возможность получать список всех объектов и их общее количество.  
- Добавил в БД таблицы для office, landplot.

09.04.2025 [CATALOG]:
- Сделал прототип каталога. Объявления создаются, но поля нужно донастроить. 
- РЕДИС: если тестим локально - кэшируется через джанго / если в докере - нужно переключить переменную в .енв на False, а затем билдить. 

06.04.2025 [ГОТОВАЯ АВТОРИЗАЦИЯ]:
- Добавлен Session Check. Юзер не вылетает при обновлении сайта.  
- Работа над авторизацией завершена!

22.03.2025: 
- Убрал .env.docker, переместил хост редиса в обычный .енв
- Убрал слеши из путей в файле urls.py, в настройках поставил APPEND_SLASH = False, чтобы Django не добавлял автоматически слеш в конце URL, если его нет. 
- Редис всегда работает из контейнера. Смирись. 

09.03.2025:
- Пофиксил сборку докера, устранив конфликт баз данных. 
- Добавил python-dotenv в requirements, т.к без него сборка не происходила

08.03.2025:
- Добавлен .env.example (.env copy) 

07.03.2025:
- Добавлена обработка ЭП /refresh: добавлен файл "refresh.py" с логикой, а также путь. 
- Была проблема: Глобальная аутентификация JWTAuthentication отклоняла refresh token, так как он не был в settings.pt/AUTH_TOKEN_CLASSES.
  Как решили: Отключили аутентификацию и права в RefreshTokenView, чтобы вручную валидировать refresh token.  

06.03.2025:
- Изменена структура проекта: теперь масштабирование будет легче (из-за этого неминуемо были проблемы с импортами, но все пофикшено);
- Создана новая БД (это пришлось сделать из-за новой структуры, хотя я сомневаюсь, что это правильно);
- Добавлена почва для внедрения следующих приложений.

04.03.2025:
- Удалены миграции из гитигнора
- Изменен метод sign-up-activate на POST;
- Передаем токен в body.
- Чтобы протестить регистрацию, нужно запускать редис в докере. 

04.03.2025:
- Исправлен new_password на password в vews.py; 
- requirements.txt вытащил на уровень проекта (в корень);
- Изменен и модернизирован docker-compose.yml: изменена дефолтная БД на testdb, добавлен healthcheck, к композу подключен REDIS;

Особенности версии: 
- Все необходимые части сервиса (БД, бэк, фронт, редис) запускаются в едином контейнере realtor;
- Локация редиса изменена (см. settings.py) - исправлена ошибка обращения к редису; 
- Приправлена слезами

